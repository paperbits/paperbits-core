/**
 * Settings required to bootstrap Google Tag Manager agent.
 */
export interface GoogleTagManagerSettings {
    /**
     * Google Tag Manager container ID, e.g. GTM-XXXXXXX
     */
    containerId: string;
}